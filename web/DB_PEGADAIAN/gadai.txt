CREATE TABLE  "GADAI" 
   (	"ID_GADAI" VARCHAR2(5) NOT NULL ENABLE, 
	"ID_CUSTOMER" VARCHAR2(5) NOT NULL ENABLE, 
	"ID_BARANG" VARCHAR2(5) NOT NULL ENABLE, 
	"JUMLAH_PINJAMAN" NUMBER(15,0), 
	"TANGGAL_PINJAMAN" DATE, 
	"JATUH_TEMPO" DATE, 
	"SISA" NUMBER(15,0), 
	"TANGGAL_DITERIMA" DATE, 
	 CONSTRAINT "GADAI_PK" PRIMARY KEY ("ID_GADAI") ENABLE
   ) ;ALTER TABLE  "GADAI" ADD CONSTRAINT "GADAI_FK" FOREIGN KEY ("ID_BARANG")
	  REFERENCES  "BARANG" ("ID_BARANG") ENABLE;ALTER TABLE  "GADAI" ADD CONSTRAINT "GADAI_FK2" FOREIGN KEY ("ID_CUSTOMER")
	  REFERENCES  "CUSTOMER" ("ID_CUSTOMER") ENABLE;

CREATE OR REPLACE TRIGGER  "BI_GADAI" 
  before insert on "GADAI"               
  for each row  
begin   
  if :NEW."ID_GADAI" is null then 
    select "GADAI_SEQ".nextval into :NEW."ID_GADAI" from dual; 
  end if; 
end; 

/
ALTER TRIGGER  "BI_GADAI" ENABLE;